NODE_ENV=production
VUE_APP_API_URL="http://localhost:5001"
# Origin Cypress drives the app from; the API needs it to allow the app's
# cookie-authenticated cross-origin requests through CORS.
PYDATALAB_APP_URL="http://localhost:8081"
